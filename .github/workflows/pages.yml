name: Materialize INEP batch through existing workflow channel

on:
  push:
    branches: ["main"]

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 2
      - name: Execute embedded INEP materializer
        shell: bash
        run: |
          python - <<'PY'
          import pathlib, textwrap
          p=pathlib.Path('.github/workflows/materialize-inep-20260818.yml')
          s=p.read_text(encoding='utf-8')
          marker="python - <<'PY'\n"
          block=s.split(marker,1)[1].split("\n          PY",1)[0]
          code=textwrap.dedent(block)
          exec(compile(code,str(p),'exec'))
          PY
      - name: Validate and rebuild
        run: |
          python scripts/validate_brazil_scope.py
          python scripts/validate_product_catalog.py
          python scripts/build_catalog.py
          python scripts/audit_link_roles.py --write
          python scripts/validate_vitrine.py
          python scripts/build_site_artifact.py
          git diff --check
          python - <<'PY'
          import csv
          def read(p):
            with open(p,encoding='utf-8',newline='') as f:return list(csv.DictReader(f))
          rs=read('data/data_resources.csv'); ps=read('data/data_products.csv'); ds=read('data/product_distributions.csv')
          assert any(x['resource_id']=='DR0125' for x in rs)
          assert all(any(x['product_id']==f'DP{i:06d}' and x['resource_id']=='DR0125' for x in ps) for i in range(735,745))
          assert all(any(x['distribution_id']==f'DD{i:06d}' for x in ds) for i in range(766,776))
          rid={x['resource_id'] for x in rs}; pid={x['product_id'] for x in ps}
          assert all(x['resource_id'] in rid for x in ps)
          assert all(x['product_id'] in pid for x in ds)
          assert len({x['resource_id'] for x in rs})==len(rs)
          assert len({x['product_id'] for x in ps})==len(ps)
          assert len({x['distribution_id'] for x in ds})==len(ds)
          print('COUNTS',len(rs),len(ps),len(ds))
          PY
      - name: Restore permanent workflow clean temporary files and commit
        shell: bash
        run: |
          git config user.name 'vitrine-catalog-bot'
          git config user.email 'vitrine-catalog-bot@users.noreply.github.com'
          git show HEAD^:.github/workflows/pages.yml > .github/workflows/pages.yml
          rm -f .github/workflows/materialize-inep-20260818.yml
          git add .github/workflows/pages.yml .github/workflows/materialize-inep-20260818.yml data/data_resources.csv data/data_products.csv data/product_distributions.csv data/brazil_scope_priorities.json data/data_products.json data/link_role_audit.json data/data_quality_report.json 2>/dev/null || true
          git status --short
          git commit -m 'Expand verified INEP education catalog'
          git push origin HEAD:main
